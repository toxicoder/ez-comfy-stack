"""Package purpose: Docs-site JSON includes (command builder + glossary)."""

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "includes",
    srcs = glob(
        ["*"],
        allow_empty = True,
        exclude = ["BUILD.bazel"],
    ),
)
