# Python tooling for documentation generators and hermetic tests.
# The site itself is Fumadocs on Next.js (docs-site/). Generators are stdlib.
# pytest lives in tests/requirements.txt (the 100% coverage gate).
