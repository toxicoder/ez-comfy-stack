"""Package purpose: Optional studio-ui board server sources."""

package(default_visibility = ["//visibility:public"])

filegroup(
    name = "sources",
    srcs = glob(
        ["**/*.py"],
        allow_empty = True,
    ),
)
