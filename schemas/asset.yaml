# ez.asset.v1 — Asset Bible record (outputs under COMFY_OUTPUT_DIR/assets, never MODELS_DIR).
# Restricted YAML subset (stdlib parser in scripts/lib/asset_bible.py; no PyYAML).
schema: ez.asset.v1
id: mug-cobalt-chipped
kind: object              # object | character | building | set | scene | material | hdri
prompt: "chipped cobalt ceramic mug, studio product lighting, white sweep"
seed: 42
pipeline: klein-trellis2  # klein-trellis2 | klein-edit | bpy-primitive | mcp-construct | scene-assemble
parent: null
takes: [v001, v002]
files: {preview, hero_glb, views}
tags: [prop, kitchen]
license: operator-output
ready: true
