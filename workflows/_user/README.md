This directory is a local convention only. Live private graphs live on `${COMFY_OUTPUT_DIR}/comfy-user/default/workflows/_user/`. Do not commit contents.

Save As `_user/` in Comfy. If you Save a new App or an edited lab graph under live `_lab/`, the next `start` moves extras into `_user/` and copies edits into `_user/_rescued/`, then restores the git catalog in `_lab/`.
