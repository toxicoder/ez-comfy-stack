# Whitelist-only context — never bake secrets, models, or host files.
# (Parent repo .env, MODELS_DIR, and HF caches must not appear here.)
*
!Dockerfile
!entrypoint.sh
!install-comfy.sh
!patch_get_free_memory.py
